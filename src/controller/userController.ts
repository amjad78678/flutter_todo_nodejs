import { Request, Response } from "express";
import asyncErrorHandler from "../config/asyncErrorHandler";

const postRegister = asyncErrorHandler(
  async (req: Request, res: Response) => {

    
  }
);

export { postRegister };
